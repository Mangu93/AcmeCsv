apply plugin: "war"

bootWar {
    mainClassName = "com.amp.acmecsv.AcmeCsvApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
